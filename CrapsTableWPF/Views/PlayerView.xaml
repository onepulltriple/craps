<UserControl x:Class="CrapsTableWPF.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrapsTableWPF.Views"
             xmlns:viewmodels="clr-namespace:CrapsTableWPF.ViewModels"
             mc:Ignorable="d" 
             Width="200" Height="100">
    
    <UserControl.DataContext>
        <viewmodels:PlayerViewModel />
    </UserControl.DataContext>

    <Border Style="{StaticResource RoundedBorderStyle}">
        <StackPanel>
            <TextBlock Text="{Binding Name}" Style="{StaticResource TitleTextStyle}"/>
            <Label Content="{Binding Purse}"/>
        </StackPanel>
    </Border>
</UserControl>
