<UserControl x:Class="CrapsTableWPF.Views.SetCrapsTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrapsTableWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400"
             Background="{StaticResource CasinoGreenBrush}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Style="{StaticResource PopUpWindowBodyTextStyle}"
                   Text="Please set the table minimum:"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding enteredTableMinimum}"/>

    </Grid>
</UserControl>
