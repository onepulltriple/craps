<UserControl x:Class="CrapsTableWPF.Views.GameEventFeedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:CrapsTableWPF.Views"
             xmlns:viewmodels="clr-namespace:CrapsTableWPF.ViewModels"
             xmlns:designtime="clr-namespace:CrapsTableWPF.DesignTime_Classes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=designtime:DesignGameEventFeedViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="400" d:DesignWidth="800"
             >
    <ItemsControl 
        ItemsSource="{Binding DisplayedEvents}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Text}">
                    <TextBlock.Style>
                        <Style 
                            TargetType="TextBlock"
                            BasedOn="{StaticResource GameEventFeedNormalTextBlockStyle}"
                            >
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Type}"
                                             Value="DiceRoll">
                                    <Setter Property="Foreground" Value="{StaticResource DiceRollTextBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}"
                                             Value="Outcome">
                                    <Setter Property="Margin" Value="20,0,0,0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}"
                                             Value="Message">
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Type}"
                                             Value="Warning">
                                    <Setter Property="Foreground" Value="{StaticResource WarningTextBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    
</UserControl>
