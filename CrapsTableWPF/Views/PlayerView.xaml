<UserControl x:Class="CrapsTableWPF.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrapsTableWPF.Views"
             xmlns:viewmodels="clr-namespace:CrapsTableWPF.ViewModels"
             mc:Ignorable="d" 
             Width="360" Height="100">
    
    <UserControl.DataContext>
        <viewmodels:PlayerViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
            Style="{StaticResource PlayerHeadingStyle}"
            Text="{Binding Name}" 
            d:Text="player.Name"
            />
        <TextBlock Grid.Row="1"
            Style="{StaticResource PlayerBodyTextStyle}"
            Text="{Binding Purse}"
            d:Text="player.Purse"
            />
    </Grid>
</UserControl>
